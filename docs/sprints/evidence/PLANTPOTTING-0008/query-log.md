# Query log — exact executed queries, retrieval dates, rate-limit notes

Retrieval date for all entries unless stated otherwise: **2026-06-05**.
Query templates and the "usable" definition: see `methodology.md`. Controlled vocabulary: see
`species-targets.md`. Each entry records the exact URL probed, the returned count field, and any
caveat. Probes are read-only count probes (`per_page=0` / `limit=0`), throttled, run off-device.

> **Status:** Phase 1 fills this file. Each species gets a block below as its rows land in
> `source-counts.csv`.

---
