# PLANTPOTTING-0011 — review feedback

Drop bugs, screenshots, and notes here for `/sprint-review`. Created at execution time so files
can be added immediately.

Note: this accuracy sprint's headline deliverables (BEFORE/AFTER scorecard, preprocessing
ADOPT/DROP, abstention tuning) are produced by the **CI-only** GMD `pixel6Api34` run — see
`docs/sprints/evidence/PLANTPOTTING-0011/`.
